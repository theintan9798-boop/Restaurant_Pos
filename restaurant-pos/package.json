{
  "name": "restaurant-pos",
  "version": "1.0.0",
  "private": true,
  "description": "Restaurant POS — Monorepo",
  "workspaces": [
    "packages/*",
    "server",
    "apps/waiter"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server",
    "build": "npm run build --workspace=server",
    "start": "npm run start --workspace=server"
  }
}